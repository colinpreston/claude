# <Client Name> — <Role Name>

*One desk's brief. Read alongside `<ClientName>_Project_Brain.md`. Keep this to one page — if it's growing much longer, that's a sign the role needs splitting or the extra detail belongs in the role's memory log instead.*

**Instructions:** *<Write this in the imperative, second person, and make it concrete to this engagement — not a generic job description. E.g. for a Lead UX Researcher on a specific project: "You plan and run the stakeholder interviews and usability sessions for <client>. Before proposing a research plan, check the Delivery Manager's log for anything already flagged as time-critical. Turn raw sessions into synthesised findings within a few days of running them — don't let transcripts pile up unsynthesised.">*

**Context needed from the Project Brain:** <which sections actually matter to this role — not all of them necessarily do>, plus the Research Repository as standing context.

**Collaborates with:**
- Reads from: <which other roles' logs this role checks before acting, and why>
- Feeds into: <which other roles depend on this role's output, and what they use it for>

*(Be specific about direction and reason — "reads the Lead UX Researcher's findings log before starting any wireframe" is useful; "collaborates with everyone" is not.)*

**Tools / references:** <what this role actually needs access to — client files, a specific research repository, a design tool, the live product, etc.>

**Skills to check automatically:** <Claude skills that match this role's day job — installed ones by name, plus anything recommended from the external skill catalogs (see SKILL.md Step 4) that isn't installed yet.>

**Memory:** `<ClientName>_<RoleName>_Log.md` — structured in three zones per `<ClientName>_Project_Brain.md` §5 (Active Notes / Digest / Archive), not one endlessly-growing log. **Active Notes** gets appended every time this role runs (what it was given, what it produced, what's still open); when something resolves — *<a concrete example: a flow gets approved, a study gets synthesised, a priority gets decided>* — it collapses to one line in the **Digest** and the full entry moves to the **Archive**. Read Active Notes and Digest by default; pull the Archive only to trace history. If this role collects research or data, also log a synthesized entry in `<ClientName>_Research_Repository.md`'s Active Notes.

**Handoff:** <what this role can draft and run with on its own vs. what needs Colin's send/edit/skip before it moves — tie back to the Project Brain's fences>

**Cadence:** <ad hoc / weekly / tied to a specific project phase — whatever's realistic for this role on this engagement>
